apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcp-task-server-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mcp-task-server-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcp-task-server-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
