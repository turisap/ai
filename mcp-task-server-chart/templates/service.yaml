apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-task-server-chart.fullname" . }}
  labels:
    {{- include "mcp-task-server-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-task-server-chart.selectorLabels" . | nindent 4 }}
